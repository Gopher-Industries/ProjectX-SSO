import styled from "styled-components";
import {motion} from "framer-motion";

export const LoginContainer = styled(motion.div)`
  margin: 20px 0;
  width:270px;
`

export const LoginLoading = styled(motion.div)`
  > img {
    margin-top:20px;
    width:50px;
  }
`
